spring:
  application:
    name: SillaRi
  datasource:
    url: "jdbc:h2:mem:sillari"
    username: sillari
    password:
    driver-class-name: org.h2.Driver
  jooq:
    sql-dialect: H2
#  h2:
#    console:
#      enabled: true
#      path: "/h2"
  flyway:
    enabled: true
    baselineOnMigrate: true
server:
  port: 8080
  servlet: # 2.0
    context-path: /api
